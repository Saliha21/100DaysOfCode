// The sixth day lesson - Identifiers 
// Saliha Almalki


/* Identifiers identify variables, labels, Function.
 Identifiers are not variables. */

var day = 6; // the variable day is identifier
var lesson = " Identifiers."; 
function experiment(){  // the function experiment is identifier
	document.write("day "+ day);
  document.write(" lesson is" +lesson);
}

experiment();

