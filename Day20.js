// 20-100 * loops - while *
//Saliha Almalki
var number = 20;
while (number <= 100) { // "While" is used to repeat a specific block, until a condition is false.
 document.write(number+"<br>");
  number+= 10; // increment by 10.
}
